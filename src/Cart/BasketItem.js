import {IconButton, ListItem, Typography} from "@material-ui/core";

export default function BasketItem({removeCart}) {
    return (
        <ListItem>
            <Typography
                variant='body1'>
                {/*{item.name}*/}
            </Typography>
            {/*<IconButton*/}

            {/*</IconButton>*/}
        </ListItem>
    )
}